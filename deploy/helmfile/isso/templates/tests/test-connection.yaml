apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "isso.fullname" . }}-test-connection"
  labels:
    {{- include "isso.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "isso.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
